#!/usr/bin/env python3
"""
Personal OS — xw mirror: 语言镜像分析 (Layer 04 MVP)
用法: xw mirror → 生成表达画像报告
"""
import sys
sys.path.insert(0, "/Volumes/13384923891/hermes-agent/context-system")

from core.mirror import run_mirror

print("🪞 语言镜像分析中...")
report_path = run_mirror()
print(f"✅ 表达画像报告已生成: {report_path}")
print()
print("在 Hermes 对话中说「执行镜像分析」，我会读取数据并进行深度解读。")
